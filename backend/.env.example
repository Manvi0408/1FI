# Optional. If set, the server connects to this MongoDB instance.
# If left unset/empty, the server boots an in-memory MongoDB automatically,
# so the app runs with zero manual database setup.
MONGODB_URI=

# Port the API server listens on.
PORT=5000
