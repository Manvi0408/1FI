{
  "name": "emi-financing-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack EMI financing product (Snapmint-style): React + Tailwind frontend, Express + MongoDB backend.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
